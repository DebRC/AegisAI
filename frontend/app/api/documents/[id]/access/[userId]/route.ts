import { NextResponse } from "next/server";
import { AegisApiError, aegisApi } from "../../../../../../lib/server/api-client";
import { requireAccessToken } from "../../../../../../lib/server/session";
export async function PUT(request: Request, context: { params: Promise<{ id: string; userId: string }> }): Promise<NextResponse> { const { id, userId } = await context.params; const level = (await request.json().catch(() => ({})) as { accessLevel?: unknown }).accessLevel; if (!/^\d+$/.test(id) || !/^\d+$/.test(userId) || (level !== "read" && level !== "write")) return NextResponse.json({ detail: "Invalid sharing request" }, { status: 422 }); try { const token = await requireAccessToken(); if (!token) return NextResponse.json({ detail: "Authentication required" }, { status: 401 }); return NextResponse.json(await aegisApi.grantDocumentAccess(token, Number(id), Number(userId), level)); } catch (e) { return NextResponse.json({ detail: e instanceof AegisApiError ? e.message : "Sharing unavailable" }, { status: e instanceof AegisApiError ? e.status : 503 }); } }
export async function DELETE(_: Request, context: { params: Promise<{ id: string; userId: string }> }): Promise<NextResponse> { const { id, userId } = await context.params; try { const token = await requireAccessToken(); if (!token) return NextResponse.json({ detail: "Authentication required" }, { status: 401 }); await aegisApi.revokeDocumentAccess(token, Number(id), Number(userId)); return new NextResponse(null, { status: 204 }); } catch (e) { return NextResponse.json({ detail: e instanceof AegisApiError ? e.message : "Sharing unavailable" }, { status: e instanceof AegisApiError ? e.status : 503 }); } }
