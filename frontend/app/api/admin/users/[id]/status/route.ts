import { NextResponse } from "next/server";
import { AegisApiError, aegisApi } from "../../../../../../lib/server/api-client";
import { requireAccessToken } from "../../../../../../lib/server/session";
export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }): Promise<NextResponse> { const { id } = await context.params; const body = await request.json().catch(() => ({})) as { isActive?: unknown }; if (!/^\d+$/.test(id) || typeof body.isActive !== "boolean") return NextResponse.json({ detail: "Invalid status request" }, { status: 422 }); try { const token = await requireAccessToken(); if (!token) return NextResponse.json({ detail: "Authentication required" }, { status: 401 }); return NextResponse.json(await aegisApi.setAdminUserStatus(token, Number(id), body.isActive)); } catch (e) { return NextResponse.json({ detail: e instanceof AegisApiError ? e.message : "User update unavailable" }, { status: e instanceof AegisApiError ? e.status : 503 }); } }
